// Common time-related constants
// MINUTE_MS: number of milliseconds in one minute
const MINUTE_MS = 60 * 1000;
 const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export = {
  MINUTE_MS, EMAIL_REGEX
};
