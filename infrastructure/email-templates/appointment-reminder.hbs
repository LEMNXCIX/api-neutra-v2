<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appointment Reminder - {{tenant.tenantName}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .header {
            background-color: {
                    {
                    tenant.primaryColor
                }
            }

            ;
            padding: 30px 20px;
            text-align: center;
        }

        .logo {
            max-width: 150px;
            height: auto;
        }

        .content {
            padding: 40px 30px;
        }

        h1 {
            color: #333;
            font-size: 24px;
            margin-bottom: 20px;
        }

        p {
            margin-bottom: 15px;
            color: #666;
        }

        .reminder-box {
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 20px;
            margin: 20px 0;
            border-radius: 5px;
        }

        .appointment-card {
            background-color: #f8f8f8;

            border-left: 4px solid {
                    {
                    tenant.primaryColor
                }
            }

            ;
            padding: 20px;
            margin: 20px 0;
            border-radius: 5px;
        }

        .appointment-card .detail {
            margin-bottom: 12px;
        }

        .appointment-card .label {
            font-weight: bold;
            color: #333;
            display: inline-block;
            width: 120px;
        }

        .appointment-card .value {
            color: #666;
        }

        .button {
            display: inline-block;
            padding: 12px 30px;

            background-color: {
                    {
                    tenant.primaryColor
                }
            }

            ;
            color: #ffffff !important;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 10px 20px 0;
            font-weight: bold;
        }

        .button.danger {
            background-color: #dc3545;
        }

        .footer {
            background-color: #f8f8f8;
            padding: 20px 30px;
            text-align: center;
            font-size: 12px;
            color: #999;
        }

        .footer a {
            color: {
                    {
                    tenant.primaryColor
                }
            }

            ;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            {{#if tenant.tenantLogo}}
            <img src="{{tenant.tenantLogo}}" alt="{{tenant.tenantName}}" class="logo">
            {{else}}
            <h2 style="color: #fff; margin: 0;">{{tenant.tenantName}}</h2>
            {{/if}}
        </div>

        <div class="content">
            <h1>🔔 Appointment Reminder</h1>

            <p>Hi {{userName}},</p>

            <div class="reminder-box">
                <p style="margin: 0;"><strong>This is a friendly reminder</strong> about your upcoming appointment
                    tomorrow.</p>
            </div>

            <div class="appointment-card">
                <div class="detail">
                    <span class="label">Service:</span>
                    <span class="value">{{serviceName}}</span>
                </div>
                <div class="detail">
                    <span class="label">Staff:</span>
                    <span class="value">{{staffName}}</span>
                </div>
                <div class="detail">
                    <span class="label">Date:</span>
                    <span class="value">{{appointmentDate}}</span>
                </div>
                <div class="detail">
                    <span class="label">Time:</span>
                    <span class="value">{{appointmentTime}}</span>
                </div>
                <div class="detail">
                    <span class="label">Duration:</span>
                    <span class="value">{{duration}} minutes</span>
                </div>
            </div>

            <a href="{{tenant.websiteUrl}}/appointments/{{appointmentId}}" class="button">View Details</a>
            <a href="{{tenant.websiteUrl}}/appointments/{{appointmentId}}/cancel" class="button danger">Cancel
                Appointment</a>

            <p style="margin-top: 30px;"><strong>Please remember to:</strong></p>
            <ul style="margin-left: 20px; margin-bottom: 20px; color: #666;">
                <li>Arrive 5-10 minutes early</li>
                <li>Bring any necessary documents or information</li>
                <li>Cancel at least 24 hours in advance if you can't make it</li>
            </ul>

            <p>We're looking forward to seeing you!</p>
            <p><strong>The {{tenant.tenantName}} Team</strong></p>
        </div>

        <div class="footer">
            <p>© 2025 {{tenant.tenantName}}. All rights reserved.</p>
            <p>
                <a href="{{tenant.websiteUrl}}">Visit our website</a> |
                <a href="mailto:{{tenant.supportEmail}}">Contact Support</a>
            </p>
        </div>
    </div>
</body>

</html>